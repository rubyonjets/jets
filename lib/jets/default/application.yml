---
project_name: proj
env_extra: # Optional. Allows creation of multiple instances of env. Any random identifer. IE: 1,2,abc,xzy
timeout: 10
runtime: nodejs6.10
memory_size: 1536
cors: true
# environment variables in the application.yml file apply to all lambda functions:
# No need to set JETS_ENV in application.yml, JETS_ENV is automatically always set.
# environment:
#   VAR1: value1
#   VAR2: value2
