#!/usr/bin/env ruby

puts "bin/lam RUBY_VERSION #{RUBY_VERSION}"

# Trap ^C
Signal.trap("INT") {
  puts "\nCtrl-C detected. Exiting..."
  sleep 1
  exit
}

$:.unshift(File.expand_path("../../lib", __FILE__))
require "lam"
require "lam/cli"

Lam::CLI.start(ARGV)
