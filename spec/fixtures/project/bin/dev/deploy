#!/bin/bash

# Hacky wrapper script so I do not forget to update the jets remote gem

bundle update
time jets deploy

# grep '^gem "jets", git:' Gemfile
# if [ $? -eq 0 ] ;then
#   # echo "RIGHT: remote gem configured"
#   bundle update
#   # time jets deploy
# else
#   echo "WRONG: local gem configured"
#   echo "You've got the jets gem configured to a local path."
#   echo ""
#   echo "1. cd jets ; git ci -m 'update jets' -a ; git push"
#   echo "2. Update Gemfile to point to the remote git in this project"
#   echo "3. bin/deploy again"
# fi
