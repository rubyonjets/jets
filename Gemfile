source "https://rubygems.org"

# Specify your gem dependencies in jets.gemspec
gemspec

# adding development gems in here because adding it in the gemspec
# the gems still get installed by bundler even with
#   bundle install --without development test
# group :development, :test do
#   gem "byebug"
#   gem "bundler"
#   gem "rake"
#   gem "rspec"

#   # ruby_dep-1.5.0 requires ruby version >= 2.2.5, which is incompatible with the current version, ruby 2.2.2p95
#   # gem "guard"
#   # gem "guard-bundler"
#   # gem "guard-rspec"
#   # gem "codeclimate-test-reporter", group: :test, require: nil
# end
